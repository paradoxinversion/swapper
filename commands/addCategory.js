const getSwapList = require("../utilities/getSwapList");
const addCategory = function(){
  
};
// See if the current category exists in the swap List

// Create it if not

// Otherwise inform the user the category already exists
